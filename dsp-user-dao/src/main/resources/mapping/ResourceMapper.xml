<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dsp.user.mapper.PermissionMapper">

	<select id="findAllPermissions" resultType="com.dsp.model.Permission">
		select * from permissions
	</select>

	<select id="findUserPermissionsById" parameterType="java.lang.Integer" resultType="com.dsp.model.Permission">
		select p.* from users u, roles r, permissions p, users_roles ur, roles_permissions rp where u.id=#{id}
		and u.id=ur.user_id and r.rId=ur.role_id and r.rId=rp.role_id and p.pId=rp.permission_id
	</select>

</mapper>
